// PDF.js Worker - Simplified version for browser compatibility
// Workaround to prevent worker loading errors in development
self.addEventListener('message', function(e) {
  console.log('PDF.js worker message received:', e.data);
  // Send back a simple response
  self.postMessage({ type: 'ready' });
});

console.log('PDF.js worker loaded');