// API Configuration for iTakecare Catalog
// Configure these URLs to point to your Leazr API endpoints

export const API_CONFIG = {
  // Base URL of your Leazr API
  BASE_URL: 'https://your-leazr-api.com/api',
  
  // Endpoints
  ENDPOINTS: {
    CATALOG: '/catalog',
    PRODUCTS: '/products',
    PRODUCT_BY_SLUG: '/products/slug',
    CATEGORIES: '/categories',
    BRANDS: '/brands',
    COMPANY: '/company',
    CART: '/cart',
    ORDER: '/order',
  },
  
  // Headers
  HEADERS: {
    'Content-Type': 'application/json',
    'Accept': 'application/json',
  },
  
  // Add your API key or authentication headers here
  // AUTH_HEADER: 'Bearer your-api-key',
};

// Helper to build full URLs
export const buildUrl = (endpoint: string, params?: Record<string, string>) => {
  let url = `${API_CONFIG.BASE_URL}${endpoint}`;
  
  if (params) {
    const searchParams = new URLSearchParams(params);
    url += `?${searchParams.toString()}`;
  }
  
  return url;
};

// Default request options
export const getRequestOptions = (method: string = 'GET', body?: any) => {
  const options: RequestInit = {
    method,
    headers: API_CONFIG.HEADERS,
  };
  
  if (body) {
    options.body = JSON.stringify(body);
  }
  
  // Add authentication header if needed
  // options.headers = {
  //   ...options.headers,
  //   'Authorization': API_CONFIG.AUTH_HEADER,
  // };
  
  return options;
};